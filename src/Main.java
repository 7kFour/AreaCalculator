public class Main {

    public static void main(String[] args) {

        AreaCalculator.area(5.0); //78.53975
        AreaCalculator.area(-1); // -1 so no output
        AreaCalculator.area(5.0, 4.0); //20.0
        AreaCalculator.area(-1.0, 4.0); // -1 so no output
    }
}
